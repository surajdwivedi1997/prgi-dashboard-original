package com.example.prgi.repo;

import com.example.prgi.domain.NewRegNewApplication;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NewRegNewApplicationRepository extends JpaRepository<NewRegNewApplication, Long> {}
